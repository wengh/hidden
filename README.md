Run a command without the command prompt window.

Usage:
- `hidden.exe <command>` to run `<command>` and exit immediately.
- `hiddenw.exe <command>` to run `<command>` and wait until the execution finishes.